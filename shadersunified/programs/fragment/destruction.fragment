KPDataBegin
  KPDataPosition( KFloat4, Pos, SV_POSITION)
  KPDataInOut( KFloat4, tintColor, COLOR0)
  KPDataOutColor
KPDataEnd

KPSMain(DestructionPS)
{ 
  KFloat4 outColor = KFloat4(0.0, 0.0, 0.0,  KInput(tintColor).x );

  KPSOutput(outColor);
}

KPSMain(DestructionAlphaPS)
{ 
  KFloat4 outColor = KFloat4(0.0, 0.0, 0.0, KInput(tintColor).x );

  KPSOutput(outColor);
}